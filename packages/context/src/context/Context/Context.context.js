import { createContext } from 'react';

// eslint-disable-next-line @scandipwa/scandipwa-guidelines/export-level-one
const ContextContext = createContext({});

ContextContext.displayName = 'ContextContext';
export default ContextContext;
