import { createContext } from 'react';

// eslint-disable-next-line @scandipwa/scandipwa-guidelines/export-level-one
const ShopwareContextContext = createContext({});

ShopwareContextContext.displayName = 'ShopwareContext';
export default ShopwareContextContext;
